{% extends "base.html.twig" %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block stylesheets %}

    <link href="{{ asset('csss/style.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('csss/colors/blue.css') }}">
{% endblock %}

{% block body %}
    <h1>Editer Reclamation</h1>

    {{form_start(form)}}
    {{form_widget(form)}}
    <div class="text-center">
        <button type="submit" class="btn btn-outline-primary btn-lg ">Modifier</button>
    </div>
    {{form_end(form)}}
 
        <ul class="record_actions">
    <li>
        <a href="{{ path('reclamations') }}">
            Retour à la liste
        </a>
    </li>

</ul>
{% endblock %}
