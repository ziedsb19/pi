
    <h4>Reclamations  list</h4>
    

   <div class="table-responsive">
                
                <table class="table table-striped table-hover table-bordered">
                  <thead>
                    <tr>
                     <tr>
                <th>Id</th>
                <th>Utilisateur_id</th>
                <th>Sujet</th>
                <th>Contenue</th>
                <th>Reponse</th>
                <th>Actions</th>
                  <th>Delete</th>
            </tr>
                  </thead>
        
        {% for entity in entities %}
            
                <tbody>
      <tr>
                    <td><a href="{{ path('reclamationsadmin_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                                    <td>{{ entity.utilisateurs}}</td>

                <td>{{ entity.sujet }}</td>
                <td>{{ entity.contenue }}</td>
                <td>{{ entity.reponse }}</td>
                                         
                      <td>
                        <!-- Split button -->
                        <div class="btn-group">
                          <button type="button" class="btn btn-info">Action</button>
                          <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
                            <span class="caret"></span>
                            <span class="sr-only">Toggle Dropdown</span>
                          </button>
                          <ul class="dropdown-menu" role="menu">
                            <li><a href="{{ path('reclamationsadmin_show', { 'id': entity.id }) }}">Voir</a></li>
                            <li><a href="{{ path('reclamationsadmin_edit', { 'id': entity.id }) }}">Répondre</a></li>
                            <li><a href="{{ path('reclamationsadmin_delete', { 'id': entity.id }) }}">Supprimer</a></li>

                          </ul>
                        </div>
                      </td>
                    </tr>
                  </tbody>
        {% endfor %}
     
    </table>
   </div>