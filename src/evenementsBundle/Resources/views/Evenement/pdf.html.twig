<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css"
        integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
    <title>Liste participants</title>
</head>

<body class="container">
    <h1> {{event.titre}} </h1>
    <div class="mb-2 col-md-12">
        <div>
            <span data-toggle="tooltip" data-placement="top" title="organisateur de l'evenement"><i
                    class="fas fa-user"></i>
                <strong>{{event.user.username}}</strong>
            </span>
            <span>|</span>
            <span title="nombre de vue"><i class="fas fa-eye"></i> {{event.vues}} vues</span>
            <span>|</span>
            <span title="nombre d'inscriptions"><i class="fas fa-users"></i> <span
                    id="inscri_nbr">{{inscription|length}}</span>
                inscriptions</span>
            <span>|</span>
            <span title="date de modification"><i class="fas fa-clock"></i>
                {{event.dateModification |date('d/m/y')}}</span>
        </div>
    </div>
    <hr>
    <div class="row mb-2">
        <div id="details_bar" class="col-md-4 p-3 bg-white shadow ">
            <div>
                <p class="mb-2"><span class="custom"><i class="fas fa-map-marker-alt"></span></i> {{event.adresse}}</p>
                <p class="mb-2"><span class="custom"><i class="fas fa-calendar-alt"></i> </span>
                    {{event.date | localizeddate('long', 'long', 'null', null)}}</p>
                {% if event.prix == null %}
                <p class="mb-2"><span class="custom"><span><i class="far fa-credit-card"></i></span> prix:</span>
                    Gratuit
                </p>
                {% else %}
                <p class="mb-2"><span class="custom"><i class="far fa-credit-card"></i> prix:</span> {{event.prix}} $
                </p>
                {% endif %}
                {% if event.billetsRestants == null %}
                <p class="mb-2"><span class="custom"><i class="fas fa-users"></i> places:</span> - </p>
                {% else %}
                <p class="mb-2"><span class="custom"><i class="fas fa-users"></i> places:</span>
                    {{event.billetsRestants}}
                </p>
                {% endif %}
                <p class="mb-4 row">
                    <span class="col-md-4 pr-0 custom"><i class="fas fa-tags"></i> tags: </span>
                    {% if event.categories is empty %}
                    <span> - </span>
                    {% else %}
                    <span class="col-md-8 pr-0 pl-0">
                        {% for c in event.categories %}
                        <a href="#" class="mr-1" style="color: #2f3138; font-size: 13px;">#{{c.nom}} </a>
                        {% endfor %}
                    </span>
                    {% endif %}
                </p>
            </div>
        </div>
    </div>
    <div class="row mb-2 p-4 shadow">
        <h6 class="col-md-12"><i class="fas fa-info-circle"></i> Description:</h6>
        <hr>
        {% if event.description != null %}
        <p id="new_line">{{event.description}}</p>
        {% else %}
        <p id="new_line" class="text-center mb-1 col-md-12">Pas de description pour cette evenement</p>
        {% endif %}
    </div>
    <h2>Liste des participants</h2>
    <hr>
    <table class="table mt-3">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col"><i class="fas fa-user"></i> nom prenom</th>
                <th scope="col"><i class="fas fa-address-card"></i> adresse</th>
                <th scope="col"><i class="fas fa-envelope"></i> email</th>
                <th scope="col"><i class="fas fa-phone"></i> numero tel</th>
            </tr>
        </thead>
        <tbody>
            {% for ins in inscription %}
            <tr>
                <td>{{loop.index}}</td>
                <td>{{ins.user.nomPrenom}}</td>
                <td>{{ins.user.adresse}}</td>
                <td>{{ins.user.email}}</td>
                <td>{{ins.user.numeroTel}}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</body>

</html>